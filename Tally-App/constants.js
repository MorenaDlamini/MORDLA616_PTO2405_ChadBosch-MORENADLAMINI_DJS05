/**
 * Action type constants for the tally application.
 * 
 * @module constants
 */

/** @constant {string} ADD - Action type for adding to the counter. */
export const ADD = 'ADD';

/** @constant {string} SUBTRACT - Action type for subtracting from the counter. */
export const SUBTRACT = 'SUBTRACT';

/** @constant {string} RESET - Action type for resetting the counter. */
export const RESET = 'RESET';
